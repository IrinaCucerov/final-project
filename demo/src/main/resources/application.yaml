
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/CookingRecipeOrganizer
    username: root
    password: Iriska
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none #to generate tables change to "create" / none /update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
        jdbc:
          time_zone: UTC
server:
  port: 8080
